# 2026-08-04 Data-scraping RPA 诊断

## 项目结构要点
- RPA 通过 CDP 连接本机已打开的 Chrome（9222=diagnosis，9223=monitor），操作各平台标签页。
- 后端 MySQL：RPA_DB_* 指向公网 14.103.81.32:3306（root，无 SSL）。Windows 实测连接正常。
- 选窗口机制：CDP 端口决定用哪个 Chrome；再取 hostname 第一个匹配 tab。无"按名称选窗口"。
- 单跑某平台诊断：npm run rpa:diagnosis -- --platforms=qianwen --max-tasks=1 --run-once=true --dry-run=false
- 健康检查：npm run rpa:diagnosis -- --platforms=qianwen --health-check=true（结果在日志文件，stdout 可能被吞）
- 观察真实输出：把命令重定向到 _xxx.log 再 cat（Bash 工具对 npm 子进程捕获不稳定）。

## 千问（qianwen）Windows 适配关键事实
- 9222 千问 tab 在线（www.qianwen.com/chat/...），UA=Windows NT 10.0。
- 发送按钮：旧 `bg-black-button` 哈希 class 已失效，靠 `aria-label*='发送'` 兜底。
- 回答正文选择器：`.message-select-wrapper-answer-*` 哈希 class 随会话变化（探针确认失效，真实为 message-select-content-inner-QCE5NQ），属死代码；稳定层为 `.chat-answers-card-wrap .answer-common-card .qk-markdown` / `.qk-markdown-complete`。
- 同一问题在页面有 3 个重复气泡（主对话 + 右侧导航 rn-right-navigator-item + 分享面板）→ 原 qianwenAnswerFollowsRealQuestionBubble 取最后一个副本会误判。
- 深度思考开关真实 DOM：`<button aria-label="思考" aria-pressed="false">`。
- **联网搜索入口：千问 www.qianwen.com 当前整页无常驻"联网搜索"按钮**（藏在"更多"或根本不提供）。

## 13:33 用户要求修复千问 toggle（兼容 Windows/iOS=macOS/darwin）
落地改动（5 文件，已 typecheck）：
- platforms.ts：deepThinkingControl 选择器稳健化（保留），**暂加** qianwen webSearchButtonSelectors/webSearchMenuTriggerSelectors/webSearchEnabledIndicatorSelectors。
- webSearch.ts：找不到控件改 supported:false；assertVerifiedWebSearchForZeroReferences 对 unsupported 平台零引用豁免。
- crawler.ts：recoverSubmittedQuestionResult 恢复路径用只读视角调用 assertVerifiedWebSearchForZeroReferences。
- browserDiagnostics.ts：DOM_CHANGED 正则收窄（实际 regex 未变，仅 reorder，属 no-op）。
- extractReferences.ts：QIANWEN_ANSWER_SELECTOR 移除失效哈希 class。

## 13:36 用户要求：删除千问联网搜索的操作
已执行：
- git checkout 回退 webSearch.ts / crawler.ts / browserDiagnostics.ts（整文件，均联网搜索相关）。
- platforms.ts 仅回退 webSearchButtonSelectors/webSearchMenuTriggerSelectors/webSearchEnabledIndicatorSelectors 三段，保留 deepThinkingControl。
- 保留：platforms.ts deepThinkingControl（深度思考开关，非联网搜索）、extractReferences.ts 回答选择器。
- typecheck 通过（TSC_EXIT=0）。
- 注意：回退后，resume + 零引用 + 找不到联网控件 会重新触发原 WEB_SEARCH 报错路径（不再有 DOM_CHANGED 误判豁免）。若需重跑验证千问，需关注此行为。

## 待领诊断任务（13:11 时）
diagnosis worker 共 15 条、全 brandId=17，其中千问 7 条（exec_da4d43f295ce 等）。worker 按队列领下一个千问，无"第22条"精确领取。
